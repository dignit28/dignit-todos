import TodoEntry from "./TodoEntry";
import TodoForm from "./TodoForm";

const TodoList = () => {
  return (
    <main>
      <TodoForm />
      <TodoEntry />
    </main>
  );
};

export default TodoList;
